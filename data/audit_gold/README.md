# Audit Gold Data

Place released T2/T3 human-gold files here if they are separated from the main processed dataset.

If gold labels are already included in `data/processed/`, keep this folder as documentation only and describe the relevant columns in `data/data_dictionary/README.md`.

